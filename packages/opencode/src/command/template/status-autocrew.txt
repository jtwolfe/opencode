Show the current status of the AutoCrew run.

1. Find the most recent run state directory under `.opencode/autocrew-state/`. If $1 is provided, use that run id directly.
2. Read `state.json` and `ledger.jsonl`.
3. Render a concise progress summary:
   - Run id, plan id, current phase
   - Rounds consumed / max-rounds
   - Tasks: completed / in-flight / failed-retry / failed-replan / review
   - Live worktrees with their branches and directories
   - Last 5 ledger events (most recent first)

This is a pure read — do not modify any state.
